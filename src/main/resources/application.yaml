spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test

  threads:
    virtual:
      enabled: true

  modulith:
    events:
      mongodb:
        transaction-management:
          enabled: true

logging:
  level:
    root: info